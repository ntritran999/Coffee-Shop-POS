<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="Client.Views.ConfigServerDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Client.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Width="400" RowSpacing="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#FDF8F3" Padding="20" CornerRadius="16,16,0,0">
            <Grid ColumnSpacing="15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Background="#F9E8D9" Width="44" Height="44" CornerRadius="10">
                    <FontIcon Glyph="&#xE7F4;" Foreground="#D97724" FontSize="20"/>
                </Border>

                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock Text="Cấu hình Server" FontSize="18" FontWeight="Bold" Foreground="#1A1A1A"/>
                    <TextBlock Text="Thiết lập kết nối đến máy chủ hệ thống" FontSize="13" Foreground="Gray"/>
                </StackPanel>
            </Grid>
        </Border>

        <StackPanel Grid.Row="1" Padding="20" Spacing="30">
            <StackPanel Spacing="15">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xE774;" FontSize="14" Foreground="#D97724"/>
                    <TextBlock Text="Địa chỉ Host (IP/Domain)" FontWeight="SemiBold" FontSize="14"/>
                </StackPanel>
                <TextBox x:Name="txtHost" Text="192.168.1.105" />
            </StackPanel>

            <StackPanel Spacing="15">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xE943;" FontSize="14" Foreground="#D97724"/>
                    <TextBlock Text="Cổng (Port)" FontWeight="SemiBold" FontSize="14"/>
                </StackPanel>
                <TextBox x:Name="txtPort" Text="8080" />
            </StackPanel>

            <Button x:Name="btnTest" Click="BtnTest_Click" HorizontalAlignment="Stretch" Height="45" Margin="0,10,0,0"
                    Background="#D97724" BorderThickness="0" CornerRadius="12">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xE701;" FontSize="14" Foreground="#1A1A1A"/>
                    <TextBlock x:Name="txtStatus" Text="Kiểm tra kết nối" Foreground="#1A1A1A"/>
                </StackPanel>
            </Button>
        </StackPanel>
    </Grid>
</ContentDialog>
