<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Client.Views.POSPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Client.Views"
    xmlns:converter="using:Client.Converters"
    xmlns:models="using:Client.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <converter:FormatPrice x:Key="FormattedPrice"/>
    </Page.Resources>
    
    <Grid Margin="36" Background="#fbfaf8">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <ListView Grid.Row="0"
                      SelectionMode="Single"
                      ScrollViewer.HorizontalScrollBarVisibility="Auto"
                      ScrollViewer.HorizontalScrollMode="Enabled"
                      ScrollViewer.VerticalScrollMode="Disabled"
                      Height="60"
                      Margin="0,0,0,20"
                      ItemsSource="{x:Bind ViewModel.Categories, Mode=OneWay}"
                      SelectedItem="{x:Bind ViewModel.SelectedCategory, Mode=TwoWay}">
                <ListView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <ItemsStackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ListView.ItemsPanel>
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="Margin" Value="0, 0, 8, 0"/>
                        <Setter Property="HorizontalContentAlignment" Value="Center"/>
                        <Setter Property="VerticalContentAlignment" Value="Center"/>
                        <Setter Property="CornerRadius" Value="28"/>
                        <Setter Property="Background" Value="#EAEAEA"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListViewItem">
                                    <ListViewItemPresenter
                                        ContentTransitions="{TemplateBinding ContentTransitions}"
                                        ContentMargin="{TemplateBinding Padding}"
                                        HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                        VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
                                        CornerRadius="{TemplateBinding CornerRadius}"
                                        SelectionCheckMarkVisualEnabled="False"
                                        PointerOverBackground="#D5D5D5"
                                        SelectedBackground="#D97724"
                                        SelectedPointerOverBackground="#C76B1F"
                                        SelectedPressedBackground="#AB5D1A"
                                        SelectedForeground="White" />
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding CategoryName}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <ScrollViewer Grid.Row="1">
                <GridView ItemsSource="{x:Bind ViewModel.FilteredProducts, Mode=OneWay}" IsItemClickEnabled="True" ItemClick="Products_ItemClick">
                    <GridView.ItemContainerStyle>
                        <Style TargetType="GridViewItem" BasedOn="{StaticResource DefaultGridViewItemStyle}">
                            <Setter Property="Margin" Value="10, 5, 10, 5"/>
                        </Style>
                    </GridView.ItemContainerStyle>
                    <GridView.ItemTemplate>
                        <DataTemplate>
                            <Grid Height="200" Width="100" Background="{ThemeResource CardBackgroundFillColorDefault}" RowSpacing="16">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <Image Grid.Row="0" Source="{Binding Image}" Stretch="UniformToFill"/>
                                <StackPanel Orientation="Vertical" Grid.Row="1" Padding="10,5,10,5" Spacing="8">
                                    <TextBlock Text="{Binding Name}" TextWrapping="Wrap"/>
                                    <TextBlock Text="{Binding Price, Converter={StaticResource FormattedPrice}}" Foreground="#D97724" FontWeight="Bold"/>
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </GridView.ItemTemplate>
                </GridView>
            </ScrollViewer>
            <ProgressRing IsActive="{x:Bind ViewModel.LoadCommand.IsRunning, Mode=OneWay}"/>
        </Grid>
        <Border Background="{ThemeResource CardBackgroundFillColorDefault}" Grid.Column="1" CornerRadius="8"
                BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
            <Grid Padding="16" RowSpacing="16">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                        <FontIcon Glyph="&#xE7BF;" Margin="0,0,8,0" Foreground="#D97724"/>
                        <TextBlock Text="Đơn hàng hiện tại" FontWeight="Bold"/>
                    </StackPanel>
                </Grid>

                <Border Grid.Row="1" CornerRadius="8" Padding="16" BorderBrush="Black" BorderThickness="1">
                    <Grid>
                        <ListView x:Name="HasSelection" x:Load="{x:Bind ViewModel.HasSelection, Mode=OneWay}" SelectionMode="None" ItemsSource="{x:Bind ViewModel.SelectedProducts, Mode=OneWay}">
                            <ListView.ItemTemplate>
                                <DataTemplate x:DataType="models:BillItem">
                                    <Grid>
                                        <Border BorderBrush="Black" BorderThickness="0, 0, 0, 1" Padding="0, 0, 0, 16" Margin="0, 0, 0, 16"
                                            x:Name="Item" x:Load="{x:Bind IsNotRemoved, Mode=OneWay}">
                                        <Grid >
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="16">
                                                    <Border BorderBrush="Black" BorderThickness="1" CornerRadius="8" Padding="8" VerticalAlignment="Top" Background="#f5f4f1">
                                                    <TextBlock FontWeight="Bold" Foreground="#D97724">
                                                        <Run Text="{Binding Count, Mode=OneWay}"/><Run Text="x"/>
                                                    </TextBlock>
                                                </Border>
                                                <StackPanel Orientation="Vertical" Spacing="8">
                                                    <TextBlock Text="{Binding Detail.Name}" Style="{StaticResource BodyLargeStrongTextBlockStyle}"/>
                                                    <TextBlock>
                                                        <Run Text="{Binding Detail.Price, Converter={StaticResource FormattedPrice}}" /><Run Text="/món"/>
                                                    </TextBlock>
                                                </StackPanel>
                                            </StackPanel>
                                            <StackPanel Grid.Column="1" Orientation="Vertical" Spacing="8">
                                                <Button Command="{Binding RemoveItemCommand}" HorizontalAlignment="Right">
                                                    <FontIcon Glyph="&#xE711;" FontSize="10" FontWeight="Light"/>
                                                </Button>
                                                <TextBlock Grid.Column="1" HorizontalAlignment="Right" Text="{Binding TotalPrice, Converter={StaticResource FormattedPrice}, Mode=OneWay}" FontWeight="Bold" Foreground="#D97724"/>
                                                <TextBox Text="{x:Bind Notes, Mode=TwoWay}" PlaceholderText="Ghi chú" FontWeight="Light" FontSize="12" Width="200"/>
                                             </StackPanel>
                                        </Grid>
                                    </Border>
                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                        <TextBlock x:Name="NoSelection" x:Load="{x:Bind ViewModel.NoSelection, Mode=OneWay}" Text="Chưa có món nào được chọn" FontWeight="Light" HorizontalAlignment="Center"/>
                    </Grid>
                </Border>

                <Grid Grid.Row="2" RowSpacing="16">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <StackPanel Grid.Row="0">
                            <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="0,0,0,4">
                                <FontIcon Glyph="&#xF0E2;" Margin="0,0,8,0"/>
                                <TextBlock Text="Vị trí"/>
                            </StackPanel>
                        <ComboBox Width="200" SelectedItem="{x:Bind ViewModel.SelectedTable, Mode=TwoWay}"
                                      ItemsSource="{x:Bind ViewModel.Tables, Mode=OneWay}">
                            <ComboBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding TableName}"/>
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>
                        </StackPanel>

                        <Grid Grid.Row="1">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Tổng cộng" FontWeight="Bold"/>
                            <TextBlock Grid.Column="1" Text="{x:Bind ViewModel.TotalBillPrice, Converter={StaticResource FormattedPrice}, Mode=OneWay}" Foreground="#D97724" FontWeight="Bold" FontSize="20"/>
                        </Grid>
                    <Grid Grid.Row="2" ColumnSpacing="16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>
                        <Button IsEnabled="{x:Bind ViewModel.HasSelection, Mode=OneWay}" Command="{x:Bind ViewModel.SaveBillCommand}" CornerRadius="8" Grid.Column="0" HorizontalAlignment="Stretch" Foreground="#D97724" BorderBrush="#D97724" BorderThickness="1" Background="Transparent">
                                <StackPanel Orientation="Vertical" Padding="8" Spacing="4">
                                    <FontIcon Glyph="&#xE74E;" />
                                    <TextBlock Text="Lưu đơn"/>
                                </StackPanel>
                            </Button>
                            <Button Command="{x:Bind ViewModel.ClearOrderCommand}" CornerRadius="8" Grid.Column="1" HorizontalAlignment="Stretch" Foreground="#E53935" Background="#FFEBEE" BorderThickness="0">
                                <StackPanel Orientation="Vertical" Padding="8" Spacing="4">
                                    <FontIcon Glyph="&#xEA39;" />
                                    <TextBlock Text="Xoá đơn"/>
                                </StackPanel>
                            </Button>
                        </Grid>
                    <Button IsEnabled="{x:Bind ViewModel.HasSelection, Mode=OneWay}" Command="{x:Bind ViewModel.SavePaidBillCommand}" CornerRadius="8" Grid.Row="3" HorizontalAlignment="Stretch" Background="#D97724" Foreground="White">
                            <StackPanel Orientation="Vertical">
                                <FontIcon Glyph="&#xE930;" />
                                <TextBlock Text="Thanh toán"/>
                            </StackPanel>
                        </Button>
                    </Grid>
            </Grid>
        </Border>
        <InfoBar Grid.Column="1" VerticalAlignment="Bottom"
            IsOpen="{x:Bind ViewModel.ShowSuccess, Mode=TwoWay}"
            Severity="Success"
            Message="Thêm đơn hàng thành công." />
        <InfoBar Grid.Column="1" VerticalAlignment="Bottom"
            IsOpen="{x:Bind ViewModel.ShowFailure, Mode=TwoWay}"
            Severity="Error"
            Message="Thêm đơn hàng thất bại." />
    </Grid>
</Page>
