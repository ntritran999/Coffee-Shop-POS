<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Client.Views.ViewAITool"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Client.Views"
    xmlns:vm="using:Client.ViewModels"
    xmlns:selectors="using:Client.Views.Selector"
    xmlns:models="using:Client.Models"
    xmlns:converters="using:CommunityToolkit.WinUI.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/> 
        <DataTemplate x:Key="AiTemplate" x:DataType="models:ChatMessage">
            <Grid HorizontalAlignment="Left" Margin="0,10">
                <StackPanel Orientation="Horizontal" Spacing="10">
                    <PersonPicture BadgeGlyph="&#xEA80;" Width="32" Height="32" VerticalAlignment="Top"/>
                    <StackPanel>
                        <Border Background="Black" CornerRadius="0,12,12,12" Padding="12,8">
                            <TextBlock Text="{x:Bind Text}" TextWrapping="Wrap" MaxWidth="400"/>
                        </Border>
                        <TextBlock Text="{x:Bind Time}" FontSize="10" Foreground="Gray"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </DataTemplate>

        <DataTemplate x:Key="UserTemplate" x:DataType="models:ChatMessage">
            <Grid HorizontalAlignment="Right" Margin="0,10">
                <StackPanel HorizontalAlignment="Right">
                    <Border Background="#D35400" CornerRadius="12,0,12,12" Padding="12,8">
                        <TextBlock Text="{x:Bind Text}" Foreground="White" TextWrapping="Wrap" MaxWidth="400"/>
                    </Border>
                    <TextBlock Text="{x:Bind Time}" FontSize="10" Foreground="Gray" HorizontalAlignment="Right"/>
                </StackPanel>
            </Grid>
        </DataTemplate>

        <selectors:MessageTemplateSelector x:Key="MsgSelector"
            UserTemplate="{StaticResource UserTemplate}"
            AiTemplate="{StaticResource AiTemplate}"/>
    </Page.Resources>

    <Grid Padding="40,30" Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,25" Background="White">
            <TextBlock Text="Trợ lý thông minh" FontSize="28" FontWeight="Bold" Foreground="Black"/>
            <TextBlock Text="Sử dụng trí tuệ nhân tạo để tối ưu hóa kinh doanh và thu hút khách hàng cho quán của bạn." 
                       Foreground="Gray" Margin="0,5,0,20"/>

            <StackPanel Orientation="Horizontal" Spacing="10">
                <Button Content="Phân tích kinh doanh" Background="Gray" BorderBrush="#EEEEEE" BorderThickness="1" CornerRadius="15" Padding="15,5">
                    <Button.ContentTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <FontIcon Glyph="&#xE9D2;" FontSize="14" Margin="0,0,8,0"/>
                                <TextBlock Text="Phân tích kinh doanh" FontWeight="SemiBold"/>
                            </StackPanel>
                        </DataTemplate>
                    </Button.ContentTemplate>
                </Button>
                <Button Content="Viết mô tả món" Background="Transparent" BorderThickness="0" Foreground="Gray">
                    <Button.ContentTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <FontIcon Glyph="&#xED15;" FontSize="14" Margin="0,0,8,0"/>
                                <TextBlock Text="Viết mô tả món"/>
                            </StackPanel>
                        </DataTemplate>
                    </Button.ContentTemplate>
                </Button>
            </StackPanel>
        </StackPanel>

        <Border Grid.Row="1" Background="White" CornerRadius="15" BorderBrush="#EEEEEE" BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Padding="25,20" BorderBrush="Orange" BorderThickness="0,0,0,1" Background="OrangeRed">
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <FontIcon Glyph="&#xE8BD;" Foreground="#D35400"/>
                        <TextBlock Text="Chat với Trợ lý AI" FontWeight="Bold"/>
                    </StackPanel>
                    <TextBlock Text="Hỏi về doanh thu, chi phí hoặc dự báo xu hướng." FontSize="12" Foreground="White"/>
                </StackPanel>

                <ListView x:Name="ChatList" Grid.Row="1" Padding="20"
                          ItemsSource="{x:Bind ViewModel.Messages}"
                          ItemTemplateSelector="{StaticResource MsgSelector}"
                          SelectionMode="None">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="Padding" Value="0"/>
                        </Style>
                    </ListView.ItemContainerStyle>
                </ListView>

                <StackPanel Grid.Row="2" Padding="20">
                    <ProgressBar IsIndeterminate="True" Margin="0,0,0,10"
                                 Visibility="{x:Bind ViewModel.IsBusy, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"/>
                    <Border Background="Black" CornerRadius="25" Padding="15,5">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBox x:Name="txtInput" PlaceholderText="Ví dụ: Phân tích doanh thu tuần qua..." 
                                     Text="{x:Bind ViewModel.InputText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                     BorderThickness="0" Background="Transparent" VerticalAlignment="Center"
                                     KeyDown="txtInput_KeyDown"
                                     />
                            <Button Grid.Column="1" Command="{x:Bind ViewModel.SendMessageCommand}"
                                    Background="#D35400" Foreground="White" CornerRadius="20" Width="40" Height="40">
                                <FontIcon Glyph="&#xE724;"/>
                            </Button>
                        </Grid>
                    </Border>
                </StackPanel>
            </Grid>
        </Border>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,15,0,0" Spacing="10">
            <TextBlock Text="Gợi ý:" VerticalAlignment="Center" Foreground="Gray" Margin="0,0,5,0"/>
            <Button Content="Top 3 món bán chạy?" Style="{StaticResource AccentButtonStyle}" Background="Transparent" Foreground="Gray" BorderBrush="#DDD" CornerRadius="15"/>
            <Button Content="Dự báo doanh thu mai?" Style="{StaticResource AccentButtonStyle}" Background="Transparent" Foreground="Gray" BorderBrush="#DDD" CornerRadius="15"/>
        </StackPanel>
    </Grid>
</Page>
