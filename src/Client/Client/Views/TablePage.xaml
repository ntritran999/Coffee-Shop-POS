<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Client.Views.TablePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Client.Views"
    xmlns:model="using:Client.Models"
    xmlns:toolkit="using:CommunityToolkit.WinUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid RowSpacing="20" Margin="36" Padding="16" Background="#fbfaf8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Orientation="Vertical" Spacing="10">
                <StackPanel Orientation="Horizontal" Spacing="10">
                    <FontIcon Glyph="&#xF0E2;" />
                    <StackPanel Orientation="Vertical">
                        <TextBlock Text="So do ban" FontWeight="Bold" FontSize="24"/>
                        <TextBlock Text="Tong quan" FontWeight="Light" FontSize="12"/>
                    </StackPanel>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Spacing="5">
                    <Button CornerRadius="12" Content="Tat ca" Command="{x:Bind ViewModel.LoadCommand}"/>
                </StackPanel>
            </StackPanel>
            <StackPanel Grid.Column="1" Spacing="5">
                <Button Command="{x:Bind ViewModel.LoadCommand}">
                    <StackPanel Orientation="Horizontal" Spacing="5" Padding="10">
                        <FontIcon Glyph="&#xE72C;" />
                        <TextBlock Text="Lam moi" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
                <TextBlock FontStyle="Italic" FontWeight="Light">
                    <Run Text="Lan cap nhat cuoi: "/>
                    <Run Text="{x:Bind ViewModel.LastUpdatedText, Mode=OneWay}"/>
                </TextBlock>
            </StackPanel>
        </Grid>
        <Border Grid.Row="1" CornerRadius="12" Background="White" BorderBrush="Black" BorderThickness="1" Padding="12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="15" Margin="30,0,0,0">
                    <TextBlock>
                        <Run Text="Ban trong ("/>
                        <Run Text="{x:Bind ViewModel.EmptyCount, Mode=OneWay}"/>
                        <Run Text=")"/>
                    </TextBlock>
                    <TextBlock>
                        <Run Text="Dang dung ("/>
                        <Run Text="{x:Bind ViewModel.OccupiedCount, Mode=OneWay}"/>
                        <Run Text=")"/>
                    </TextBlock>
                    <TextBlock>
                        <Run Text="Dang don ("/>
                        <Run Text="{x:Bind ViewModel.CleaningCount, Mode=OneWay}"/>
                        <Run Text=")"/>
                    </TextBlock>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Spacing="10" Grid.Column="1">
                    <TextBlock Text="|"/>
                    <TextBlock>
                        <Run Text="Tong so: "/>
                        <Run Text="{x:Bind ViewModel.TotalCount, Mode=OneWay}"/>
                        <Run Text=" ban"/>
                    </TextBlock>
                </StackPanel>
            </Grid>
        </Border>
        <ListView Grid.Row="2" SelectionMode="None" ItemsSource="{x:Bind ViewModel.Tables, Mode=OneWay}">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="model:Table">
                    <Border Width="220" Height="260" Background="White" CornerRadius="12">
                        <Grid Padding="12">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Row="0" Text="{x:Bind TableName, Mode=OneWay}" FontWeight="Bold" FontSize="24" Margin="0,0,0,20"/>
                            <StackPanel Grid.Row="1" Orientation="Vertical" Spacing="12">
                                <Border CornerRadius="12" BorderBrush="Black" BorderThickness="1" Padding="12,4" HorizontalAlignment="Left">
                                    <StackPanel Orientation="Horizontal" Spacing="5">
                                        <FontIcon Glyph="&#xE716;" />
                                        <TextBlock Text="{x:Bind Status, Mode=OneWay}"/>
                                    </StackPanel>
                                </Border>
                                <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}">
                                    <Run Text="Trang thai: "/>
                                    <Run Text="{x:Bind Status, Mode=OneWay}"/>
                                </TextBlock>
                            </StackPanel>
                            <Button Grid.Row="2" HorizontalAlignment="Stretch">
                                <TextBlock Text="Xem don" FontSize="20"/>
                            </Button>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <toolkit:WrapPanel VerticalSpacing="30"/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
        </ListView>
        <ProgressRing Grid.Row="2" HorizontalAlignment="Right" VerticalAlignment="Top" IsActive="{x:Bind ViewModel.IsLoading, Mode=OneWay}"/>
        <InfoBar Grid.Row="2"
                 VerticalAlignment="Bottom"
                 IsOpen="{x:Bind ViewModel.HasError, Mode=OneWay}"
                 Severity="Error"
                 Message="{x:Bind ViewModel.ErrorMessage, Mode=OneWay}" />
    </Grid>
</Page>
