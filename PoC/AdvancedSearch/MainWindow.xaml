<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="AdvancedSearch.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AdvancedSearch"
    xmlns:models="using:AdvancedSearch.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Advanced Search PoC" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

        <!-- Filter Zone -->
        <Border Grid.Row="1" Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}" CornerRadius="8" Padding="16" Margin="0,0,0,20">
            <StackPanel Spacing="16">
                <Grid ColumnSpacing="16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBox x:Name="SearchKeywordBox" Header="Search Keyword (Name or Tags)" PlaceholderText="Enter product name..." Grid.Column="0"/>
                    
                    <ComboBox x:Name="CategoryComboBox" Header="Category" PlaceholderText="Select a category" HorizontalAlignment="Stretch" Grid.Column="1">
                        <!-- Populated from code behind -->
                    </ComboBox>
                </Grid>

                <Grid ColumnSpacing="16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <NumberBox x:Name="MinPriceBox" Header="Min Price" PlaceholderText="0.00" Grid.Column="0"/>
                    <NumberBox x:Name="MaxPriceBox" Header="Max Price" PlaceholderText="100.00" Grid.Column="1"/>

                    <StackPanel Orientation="Horizontal" Grid.Column="2" VerticalAlignment="Bottom" Spacing="8">
                        <Button x:Name="SearchButton" Content="Search" Style="{StaticResource AccentButtonStyle}" Click="SearchButton_Click"/>
                        <Button x:Name="ClearButton" Content="Clear Filters" Click="ClearButton_Click"/>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Border>

        <!-- Result Zone -->
        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock x:Name="ResultSummaryText" Text="Results: 0 items found" Margin="0,0,0,8" FontWeight="SemiBold"/>
            
            <ListView x:Name="ResultListView" Grid.Row="1" ItemsSource="{x:Bind FilteredProducts, Mode=OneWay}">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="models:Product">
                        <StackPanel Margin="0,8">
                            <TextBlock Text="{x:Bind Name}" FontWeight="SemiBold" FontSize="16"/>
                            <TextBlock Text="{x:Bind Category}" Foreground="Gray" FontSize="14"/>
                            <TextBlock Text="{x:Bind FormattedPrice}" FontWeight="Bold" Foreground="DarkGreen" FontSize="14"/>
                            <TextBlock Text="{x:Bind Tags}" FontStyle="Italic" FontSize="12"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </Grid>
</Window>
