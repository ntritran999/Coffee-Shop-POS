<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="MVVM.View.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MVVM.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel Name="container">
        <ComboBox Width="200" Height="40" ItemsSource="{x:Bind TableVM.Tables}" >
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock>
                   <Run Text="{Binding TableId}" Foreground="Red" FontWeight="Bold"/> - <Run Text="{Binding TableName}"/> - <Run Text="{Binding TableStatus}"/>
                        </TextBlock>
                    </StackPanel>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>
    </StackPanel>
</Page>
