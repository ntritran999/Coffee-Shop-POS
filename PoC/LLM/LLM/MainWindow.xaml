<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="LLM.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LLM"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="LLM">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <!-- Label -->
        <TextBlock Text="Nhập câu hỏi cho AI:" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
        <!-- Prompt Input -->
        <TextBox x:Name="txtPrompt" Grid.Row="1" Height="30" Margin="0,0,0,10"/>
        <!-- Result + Button -->
        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Button x:Name="btnAsk" Content="Hỏi AI" Height="35" Width="100" HorizontalAlignment="Left" Click="btnAsk_Click" Margin="0,0,0,10"/>
            <!-- Removed VerticalScrollBarVisibility property to fix XLS0413 -->
            <TextBox x:Name="txtResult" Grid.Row="1" AcceptsReturn="True" TextWrapping="Wrap" IsReadOnly="True"/>
        </Grid>
    </Grid>
</Window>
