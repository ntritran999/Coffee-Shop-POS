<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="MultiCriteriaSort.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MultiCriteriaSort"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="10" Margin="0,0,0,10">
            <Button Content="Sort by Name" Click="Sort_Name_Click" />
            <Button Content="Sort by Age" Click="Sort_Age_Click" />
            <Button Content="Sort by Salary" Click="Sort_Salary_Click" />
            
            <TextBlock x:Name="SortStatusBlock" VerticalAlignment="Center" Margin="10,0,0,0" Foreground="Gray" />
            <Button Content="Clear Sort" Click="Clear_Sort_Click" Margin="20,0,0,0" />
        </StackPanel>

        <ListView x:Name="EmployeeListView" Grid.Row="1">
            <ListView.Header>
                <Grid Padding="10" Background="LightGray">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="150" />
                        <ColumnDefinition Width="100" />
                        <ColumnDefinition Width="150" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Name" FontWeight="Bold"/>
                    <TextBlock Grid.Column="1" Text="Age" FontWeight="Bold"/>
                    <TextBlock Grid.Column="2" Text="Salary" FontWeight="Bold"/>
                </Grid>
            </ListView.Header>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="10,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150" />
                            <ColumnDefinition Width="100" />
                            <ColumnDefinition Width="150" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{Binding Name}"/>
                        <TextBlock Grid.Column="1" Text="{Binding Age}"/>
                        <TextBlock Grid.Column="2" Text="{Binding Salary}"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Window>
